## What

<!-- What does this PR change? One or two sentences. -->

## Why

<!-- Why is this change needed? Link an issue if there is one. -->

## Checklist

- [ ] `./scripts/verify.sh` passes.
- [ ] New logic in `lib/` or `hooks/` has a test (see [docs/architecture.md](../docs/architecture.md) for what belongs in which layer).
- [ ] A behavior change to an API route has an updated or new test.
- [ ] A user-facing UI change has an E2E smoke test (`pnpm e2e`) or an update to an existing one.
- [ ] Commit subject follows [Conventional Commits](../docs/contributing.md#commit-convention).

## Notes for the reviewer

<!-- Anything that isn't obvious from the diff: tradeoffs considered,
     things intentionally left out, how to manually verify the change. -->
